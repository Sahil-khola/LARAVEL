 $Request->validate([
        "userName"=>"required",
        "email"=>"required",
        "password"=>"required",
]);

for show all erorr
        {{print_r($errors)}}

  @if ($errors->any())
          @foreach ($errors->all() as $error)
              <div>
                {{$error}}
              </div>
          @endforeach
      @endif

error uske sath dikane k leya
@error("name"){{$message}} @enderror